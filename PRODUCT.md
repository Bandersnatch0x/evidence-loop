# Product

## Register

product

## Users

编程实训场景中的两类日常使用者，外加一类间接用户：

1. **编程初学者**：在课堂或机房完成 Python 函数训练，提交代码后需要即时、可解释、可复现的反馈，知道"为什么错、下一步修什么"。使用环境可能是老旧机房设备与键盘操作。
2. **实训教师/助教**：在班级学情视图中查看完成率、中位分与关注队列，获得干预建议；教师保留最终干预与成绩认定权。
3. **课程产品团队（间接）**：复用这个 Agent 模板做课程产品，需要架构透明、可检查、可替换。

界面首要服务**师生日常真实使用**：信息密度、可读性、操作效率优先；比赛评审演示是重要但次要的考量。

## Product Purpose

EvidenceLoop 是"循证实训 Agent"：用可验证证据驱动编程学习闭环——

> 提交代码 → 受限运行测试与静态检查 → 按量规生成证据与分数 → 映射薄弱知识点 → 生成下一轮修复任务 → 重新提交验证 → 更新学情

存在的理由：自动批改只给对错，大模型讲评容易幻觉和越权改分。EvidenceLoop 让**分数由测试与量规确定性计算**，模型只组织受证据约束的反馈。

成功的样子：

- 学习者提交后一眼看懂得分、哪条证据失败、下一步修什么
- 修复后重新提交，分数提升清晰可见（缺陷版 80 分 → 修复版 100 分）
- 教师不读聊天记录也能掌握班级证据队列与关注对象
- 评委/开发者能复述："分数不是模型给的，是测试证据算的"

## Brand Personality

**严谨 · 透明 · 可信**——像一份可复核的实验报告：克制、精确，每条结论都有出处。

表达方式上借鉴 **Duolingo / Khan Academy 的教育亲和力**：进度反馈清晰、状态明确、对初学者友好，用"下一步该做什么"引导而非冷冰冰的对错。

文案语气：陈述事实，不夸张、不讨好。说"空列表路径没有返回约定结果"，不说"太棒了只差一点点"。

## Anti-references

明确不要做成：

1. **聊天机器人式 AI 助教**：一大块对话框 + 流式输出。本项目立场是"为什么不是聊天机器人"。
2. **冰冷的传统 OJ 判题系统**：密集表格、红黑对错、无引导的工业风，让初学者望而生畏。
3. **花哨的营销落地页**：大渐变、玻璃拟态、滚动炫技，削弱评分工具的严谨定位。
4. **通用 AI 生成风**：千篇一律的紫色渐变大字 + 过度圆角卡片 + 每个段落一个小号大写 eyebrow 标题。

## Design Principles

1. **证据先于表达**：每个分数、每条结论旁边必须能看到它的来源（测试/静态检查、通过/失败、权重）。视觉层级的第一位永远给证据，不给装饰。
2. **闭环是可导航的结构，不是一段话**：五步评估流程（读取任务 → 受限运行 → 量规评分 → 知识匹配 → 反馈生成）是工作台的核心视觉元素，运行时逐步点亮，完成后可回看耗时。
3. **边界写在界面上**：模型不改分、教师保留认定权、本地运行器仅用于 Demo——这些限制以常驻但克制的方式呈现，不藏进文档。
4. **亲和但不低幼**：借鉴教育产品的清晰进度与鼓励式状态，但语气保持实验报告式的精确；不用表情符号当图标，不用游戏化夸张动效。
5. **日常效率优先于演示效果**：信息密度服务每天使用的师生；三分钟演示能看懂是结果，不是设计目标。

## Accessibility & Inclusion

- **WCAG AA 对比度**：正文 ≥4.5:1，大字号/加粗文本 ≥3:1。旧 UI 大量 9–10px 灰色小字不达标，重构时一并修复。
- **键盘可操作**：所有交互可 Tab 到达，焦点指示清晰可见（机房环境常见纯键盘操作）。
- **减少动画敏感**：`prefers-reduced-motion` 时关闭脉冲、点亮、过渡动画，以即时状态切换替代。
- **颜色不单独表意**（既有约束，保持）：通过/失败始终同时用图标 + 文字 + 颜色表达。
