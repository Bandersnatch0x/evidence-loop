Source check: inconclusive passages (missing-evidence), but result list itself corroborates native-dep risk — sharp issue #4498 ("Build from source fails on Node.js 24 LTS"), v24 incompatibility reports, node-gyp rebuild friction. Native-dep risk on Node 24 real, not hypothetical. Strengthens A.

---

## RECOMMENDATION: A — v1 pure-JS header/structure parsers, no new deps

**Rationale:** T-B in code-review close-out (681 green). Native dep at this stage = schedule risk, corroborated by sharp/Node-24 build failures (issue #4498) + better-sqlite3 saga precedent. A closes the only real acceptance gap (40MP/16,384px currently declared-but-unenforced) at zero dep cost, catches header-level forgeries/polyglots, and bounds pixel-bomb blast radius via 25MiB ceiling + declared-dimension caps. B's marginal gain (real decode vs header) is small because: (1) both enforce 40MP/16384, (2) both catch polyglots, (3) NEITHER satisfies §9.2 "subprocess-isolated" in-process — map iron law already defers isolation to production worker container, (4) sharp's real decode value realized in T-D thumbnails anyway. Spec-letter "真实解析 via Sharp/Khronos" stays open but is production-stage per iron law + ffprobe capability-disable precedent.

**Reject C:** under-delivers vs T-B scope ("ClamAV/Sharp/Khronos 门禁"); leaves 40MP cap unenforced = live acceptance risk.

### Risk register (Option A)

| # | Risk | Mitigation |
|---|------|------------|
| R1 | Spec-letter "真实解析 (Sharp/ffprobe/Khronos)" not met by header parse — partial §9.2 compliance | Map iron law accepts in-v1 in-process; full subprocess-isolated real parse = production worker (T-M). Document v1 boundary explicitly in T-B acceptance. |
| R2 | Pixel-bomb w/ valid small header dims but huge payload — header parse misses | 25MiB file ceiling bounds stored bytes; header dim caps (40MP/16384) bound decompressed dims; no decode in T-B = no decoder-vuln exposure; T-D thumbnail path uses sharp `limitInputPixels` as defense-in-depth. |
| R3 | Polyglot w/ forged-but-valid header — header parse misses | magic-byte gate + triangle verification (declared/actual/sniffed) already in place; header structural checks reject missing/invalid SOF/IHDR/VP8X/magic-version; ClamAV fail-closed scans payload; full decode in prod worker (T-M). |
| R4 | Sharp/gltf-validator deferred — when land? | T-M acceptance names Sharp/Khronos full subprocess validation as production worker gate (no creds, no intranet, read-only input — research §4.5 line 265). ffprobe already deferred via MEDIA_FFMPEG_PATH (licensing). Revisit B if sharp Node-24 prebuilds confirmed stable AND T-D needs in-process decode. |
| R5 | Manual GLB checks miss schema-level errors gltf-validator catches | Manual covers 12-byte header (magic 0x46546C67, version=2, length) + JSON chunk structural (asset version, scene refs exist, reject external URIs); three.js runtime also rejects malformed GLB at render; full Khronos validation in prod worker (T-M). |

### §9.2 / §9.3 DoD-4 — what remains open, and where it closes

| §9.2 component | v1 (T-B) closure | Full closure ticket |
|---|---|---|
| Image real-parse (Sharp, limitInputPixels/failOn/metadata-strip/output) | PNG/JPEG/WebP header parse (IHDR/SOF/VP8X) + 40MP/16384 caps + triangle verification | **T-M** — prod worker, subprocess-isolated Sharp |
| GLB real-parse (Khronos Validator) | 12-byte header + JSON chunk structural + external-URI reject | **T-M** — prod worker, Khronos Validator |
| Audio/video real-parse (ffprobe) | capability-disable (503 until MEDIA_FFMPEG_PATH) — already done | **T-M** — licensing-gated, MEDIA_FFMPEG_PATH |
| WebVTT | deferred | **subtitle ticket** (not T-B scope) |
| Subprocess isolation + resource caps | in-process header parse + per-file size caps (25MiB/200MiB/2GiB/250MiB/2MiB) | **T-M / production** — worker container, no creds/intranet, read-only input (iron law) |

**Net:** A ships spec-defensible v1 gate (real structure parse + enforced caps + triangle + ClamAV), defers only the subprocess-isolated full-decode to production per the established capability-disable + map-iron-law precedents. No native dep. Close-out unblocked.
