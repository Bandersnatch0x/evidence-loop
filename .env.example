# Optional. When unset, EvidenceLoop uses deterministic local feedback.
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4.1-mini

# Local demo runner. Never expose this mode directly to untrusted public traffic.
PYTHON_BIN=python
PORT=4173
